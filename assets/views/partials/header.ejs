<nav>
  <div class="logo-container">
    <img src="/images/logo.png" class="logo">
    <span class="logo-text">
      SUCCESSFIELD COLLEGE
    </span>
  </div>
  <% if(page != "login" && page != "join"){ %>
  <div class="auth-contrs">
      <% if(isLoggedIn){ %>
          <a href="/users/logout" class="auth-logout">Logout</a>
        <%} else { %>
    <a href="/users/login" class="auth-login">
      Login
    </a>
    <a href="/users/join" class="auth-signup">Signup</a>
<% } %>
  </div>
  <% } %>
</nav>
<% if(page != "login" && page != "join"){ %>
<header>
  <div class="main-menu">
  <span class="menu-button">
    <%- (new icons('menu-button-icon', 'Menu')).menu() %>
  </span>
  <ul class="menu-list">
    <% if(page !== 'home'){ %>
    <li class="menu-item"><a href="/">Home</a></li>
    <% } %>
    <li>
      <span class="course-list-button" title="Show courses">Courses</span>
      <ul class="course-list-menu">
        <% for(course of courses){ %>
          <li class="course-item"><a href="/course/<%= course.course %>"><%= course.course %></a></li>
        <% } %>
      </ul>
    </li>
    <% if(page !== 'home'){ %>
    <li class="menu-item"><a href="/verify">Verify Certificate</a></li>
    <% } %>
    <% if(page !== 'members'){ %>
    <li class="menu-item"><a href="/member">Accreditation and Memberships</a></li>
    <% } %>
    <% if(page !== 'gallery'){ %>
    <li class="menu-item"><a href="/gallery">Gallery</a></li>
    <% } %>
    <% if(page !== 'about'){ %>
    <li class="menu-item"><a href="/about">About us</a></li>
    <% } %>
    <% if(page !== 'contact'){ %>
    <li class="menu-item"><a href="/contact">Contact us</a></li>
    <% } %>
  </ul>
</div>
</header>
<% } %>